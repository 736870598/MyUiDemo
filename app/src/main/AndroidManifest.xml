<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunxy.uitestdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".record.RecordBtnActivity" />
        <activity android:name=".praise.PraiseViewActivity" />
        <activity android:name=".pos_recycle.PosRecycleViewActivity" />
        <activity android:name=".editview.DelEditViewActivity" />
        <activity android:name=".flow.FlowLayoutActivity" />
        <activity android:name=".darg.DragActivity" />

    </application>

</manifest>